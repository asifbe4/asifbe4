#########################################################################################################################################################
;
;
;   ██████╗  ██████╗ ██╗  ██╗   ██╗██████╗  █████╗ ██████╗
;   ██╔══██╗██╔═══██╗██║  ╚██╗ ██╔╝██╔══██╗██╔══██╗██╔══██╗
;   ██████╔╝██║   ██║██║   ╚████╔╝ ██████╔╝███████║██████╔╝
;   ██╔═══╝ ██║   ██║██║    ╚██╔╝  ██╔══██╗██╔══██║██╔══██╗
;   ██║     ╚██████╔╝███████╗██║   ██████╔╝██║  ██║██║  ██║
;   ╚═╝      ╚═════╝ ╚══════╝╚═╝   ╚═════╝ ╚═╝  ╚═╝╚═╝  ╚═╝
;
;
;   To learn more about how to configure Polybar
;   go to https://github.com/polybar/polybar
;
;   The README contains a lot of information
;
########################         🔱 POLYBAR July 11,2025 15:13 🔱           ###########################################################################
############################################################################################################################################################

[global/wm]
;https://github.com/jaagr/polybar/wiki/Configuration#global-wm-settings
margin-top = 0
margin-bottom = 0

; Set a DPI values used when rendering text
; This only affects scalable fonts
; Set this to 0 to let polybar calculate the dpi from the screen size.
; dpi = 
; Set a DPI values used when rendering text
; This only affects scalable fonts
; Set this to 0 to let polybar calculate the dpi from the screen size.

dpi = 0 

[settings]
;https://github.com/jaagr/polybar/wiki/Configuration#application-settings
throttle-output = 5
throttle-output-for = 10
throttle-input-for = 30
screenchange-reload = true
compositing-background = over
compositing-foreground = over
compositing-overline = over
compositing-underline = over
compositing-border = over
pseudo-transparency = true

; Define fallback values used by all module formats
format-foreground = #BFBFBF
format-background = #263238
format-underline = ${xrdb:color5:#222}
format-overline =
format-spacing =
format-padding =
format-margin =
format-offset =

[colors]
; Nord theme ============
background = #CC263238
foreground = ${xrdb:color15:#222}
foreground-alt = ${xrdb:color8:#222}
alert = #664B00
occupied = #cccccc
occupied-active = #b3c7f5
active = #5e81ac
volume-min = #a3be8c
volume-med = #ebcb8b
volume-max = #bf616a
sep = #4A4A4A

################################################################################

############################################################################################################################################################
############           TOP BAR-  MAINBAR-I3                         ########################################################################################
############################################################################################################################################################

[bar/mainbar-i3]
;https://github.com/jaagr/polybar/wiki/Configuration

monitor = ${env:MONITOR}
;monitor-fallback = HDMI1
monitor-strict = false
override-redirect = false
bottom = false
fixed-center = true
width = 100%
height = 18
;offset-x = 0%
;offset-y = 0%

background = ${colors.background}
foreground = ${colors.foreground}

; Background gradient (vertical steps)
;   background-[0-9]+ = #aarrggbb
;background-5 = #aa000000

radius = 0.0
line-size =  1
line-color = ${xrdb:color13:#555}

border-size = 0
border-left-size = 0
border-right-size = 0
border-top-size = 5
border-bottom-size = 1
border-color = ${colors.background}
padding-left = 1
padding-right = 1

module-margin-left = .75
module-margin-right = .755
		 
font-0 = "Jetbrainsmono nerd font:size=10"
font-1 = "FontAwesome:size=10"
font-2 = "Noto Sans Mayan Numerals:size=10"
font-3 = "joypixels:style=Regular:scale=10"
font-4 = "Nimbus Sans Narrow:style=Regular:size=10"
font-5 = "Cascadia Code:size=10"
font-6 = "Symbols Nerd Font:size=10"
font-7 = "Noto Sans Symbols2:size=10"
font-8 = "SauceCodePro Nerd Font:size=10"

; modules-left = launcher i3
; modules-left = apps sep que sep rof sep wal sep sel sep filesystem sep  sep memory
modules-left = apps sep que sep rof sep wal sep sel sep cpu sep
; modules-left = filesystem sep cpu-coreload sep temperature sep memory
modules-center = sep cpu-coreload sep  
;modules-right = all-updates sep date  
 modules-right = all-updates sep xweather sep squ date

separator =

dim-value = 1.0

tray-detached = false
tray-offset-x = -0
tray-offset-y = -4
tray-padding = 4
tray-maxsize = 16
tray-scale = 1.20
tray-position = right
tray-background = #A0000
tray-foreground = ${xrdb:color3:#222}
tray-underline = ${xrdb:color4:#222}

#i3: Make the bar appear below windows
wm-restack = i3
;override-redirect = true

; Enable support for inter-process messaging
; See the Messaging wiki page for more details.
enable-ipc = true

; Fallback click handlers that will be called if
; there's no matching module handler found.
click-left =  i3wm-wsnext
click-middle =  i3wm-wsprev
click-right =
scroll-up = 
scroll-down = 
double-click-left =
double-click-middle =
double-click-right =

; Requires polybar to be built with xcursor support (xcb-util-cursor)
; Possible values are:
; - default   : The default pointer as before, can also be an empty string (default)
; - pointer   : Typically in the form of a hand
; - ns-resize : Up and down arrows, can be used to indicate scrolling
cursor-click = pointer
cursor-scroll = ns-resize

############################################################################################################################################################
############       BOTTOM BAR -  MAINBAR-I3                         ########################################################################################
############################################################################################################################################################

[bar/mainbar-i3-extra]
;https://github.com/jaagr/polybar/wiki/Configuration

monitor = ${env:MONITOR}
;monitor-fallback = HDMI1
monitor-strict = false
override-redirect = false
bottom = true
fixed-center = true

width = 100%
height = 18
;offset-x = 20%
;offset-y = 0%

background = ${colors.background}
foreground = ${colors.foreground}

; Background gradient (vertical steps)
;   background-[0-9]+ = #aarrggbb
;background-5 = #aa000000

radius = 0.0
line-size =  1
line-color = ${xrdb:color13:#555}

border-size = 0
border-left-size = 0
border-right-size = 0
border-top-size = 5
border-bottom-size = 1
border-color = ${colors.background}
padding-left = 1
padding-right = 1

module-margin-left = .75
module-margin-right = .75

font-0 = "Jetbrainsmono nerd font:size=10"
font-1 = "FontAwesome:size=10"
font-2 = "Noto Sans Mayan Numerals:size=10"
font-3 = "joypixels:style=Regular:scale=10"
font-4 = "Nimbus Sans Narrow:style=Regular:size=10"
font-5 = "Cascadia Code:size=10"
font-6 = "Symbols Nerd Font:size=10"
font-7 = "Noto Sans Symbols2:size=10"
font-8 = "SauceCodePro Nerd Font:size=10"

modules-left = launcher1 sep temperature2 sep memory sep filesystem sep i3 sep launcher xwindow sep
; modules-left = filesystem sep temperature sep cpu-coreload sep temperature sep memory
; modules-left = apps sep que sep rof sep wal sep sel sep 
; modules-center =
;modules-center = sep xwindow sep  
modules-right = wireless-network sep backlight sep pavolume sep battery sep xkeyboard sep bluetooth

separator =

dim-value = 1.0

;tray-detached = false
;tray-offset-x = -0
;tray-offset-y = -4
;tray-padding = 6
;tray-maxsize = 14
;tray-scale = 1.20
;tray-position = right
;tray-background = #aaA0000
;tray-foreground = ${xrdb:color13:#555}
;tray-underline = ${xrdb:color4:#222}

;tray-detached = false
;tray-offset-x = 0
;tray-offset-y = 0
;tray-padding = 10
;tray-maxsize = 14
;tray-scale = 1.0
;tray-position = right
;tray-background = #CC2F343F

#i3: Make the bar appear below windows
wm-restack = i3
;override-redirect = true

; Enable support for inter-process messaging
; See the Messaging wiki page for more details.
enable-ipc = true

; Fallback click handlers that will be called if
; there's no matching module handler found.
click-left =
click-middle =
click-right =
scroll-up = i3wm-wsnext
scroll-down = i3wm-wsprev
double-click-left =
double-click-middle =
double-click-right =

; Requires polybar to be built with xcursor support (xcb-util-cursor)
; Possible values are:
; - default   : The default pointer as before, can also be an empty string (default)
; - pointer   : Typically in the form of a hand
; - ns-resize : Up and down arrows, can be used to indicate scrolling
cursor-click = pointer
cursor-scroll = default

############################################################################################################################################################
############                        MODULE I3                          #####################################################################################
###########################################################################################################################################################

[module/i3]
;https://github.com/jaagr/polybar/wiki/Module:-i3
type = internal/i3

; Only show workspaces defined on the same output as the bar
;
; Useful if you want to show monitor specific workspaces
; on different bars

; Reload upon receiving XCB_RANDR_SCREEN_CHANGE_NOTIFY events
screenchange-reload = true

; Default: false
pin-workspaces =true

; This will split the workspace name on ':'
; Default: false
strip-wsnumbers = false

; Sort the workspaces by index instead of the default
; sorting that groups the workspaces by output
; Default: false
index-sort = false

; Create click handler used to focus workspace
; Default: true
enable-click = true

; Create scroll handlers used to cycle workspaces
; Default: true
enable-scroll = true

; Wrap around when reaching the first/last workspace
; Default: true
wrapping-scroll = true

; Set the scroll cycle direction
; Default: true
reverse-scroll = true

; Use fuzzy (partial) matching on labels when assigning
; icons to workspaces
; Example: code;♚ will apply the icon to all workspaces
; containing 'code' in the label
; Default: false
fuzzy-match = true

;extra icons to choose from
;http://fontawesome.io/cheatsheet/
;                      ;               

ws-icon-0 = 1; :
ws-icon-1 = 2; :
ws-icon-2 = 3; :
ws-icon-3 = 4; :
ws-icon-4 = 5; :
ws-icon-5 = 6; :
ws-icon-6 = 7; :
ws-icon-7 = 8; :
ws-icon-8 = 9; :
ws-icon-9 = 10; :
ws-icon-default = " :"

; Available tags:
;   <label-state> (default) - gets replaced with <label-(focused|unfocused|visible|urgent)>
;   <label-mode> (default)
format =<label-mode><label-state>

label-mode =%mode%
label-mode-padding = 1
label-mode-foreground = #000000
label-mode-background = ${xrdb:color10:#555}

; Available tokens:
;   %name%
;   %icon%
;   %index%
;   %output%
; Default: %icon%  %name%
; focused = Active workspace on focused monitor
label-focused = %icon%%name%
label-focused-background = ${colors.background}
label-focused-foreground = ${xrdb:color14:#222}
label-focused-underline = ${xrdb:color1:#222}
label-focused-padding = 1

; Available tokens:
;   %name%
;   %icon%
;   %index%
; Default: %icon%  %name%
; unfocused = Inactive workspace on any monitor
label-unfocused = %icon%%name%
label-unfocused-padding = 1
label-unfocused-background = ${colors.background}
label-unfocused-foreground = ${xrdb:color3:#222}
label-unfocused-underline = ${xrdb:color5:#222}
;label-unfocused-underline = #565529

; visible = Active workspace on unfocused monitor
label-visible = %icon%%name%
label-visible-background = ${self.label-focused-background}
label-visible-foreground = ${xrdb:color14:#222}
label-visible-underline = ${self.label-focused-underline}
label-visible-padding = 1

; Available tokens:
;   %name%
;   %icon%
;   %index%
; Default: %icon%  %name%
; urgent = Workspace with urgency hint set
label-urgent = %icon%%name%
label-urgent-background = ${self.label-focused-background}
label-urgent-foreground = ${xrdb:color14:#222}
label-urgent-ground = #3F51B5
label-urgent-padding = 1

format-background = ${colors.background}

############################################################################################################################################################
############                       MODULES A-Z                            ##################################################################################
############################################################################################################################################################

[module/arch-updates]
type = custom/script
exec = ~/.config/polybar/scripts/check-arch-updates.sh
interval = 3000
label = %output%
label-foreground = #FFFFFF
format-foreground = ${colors.foreground}
format-background = ${colors.background}
format-prefix = ": "
format-prefix-foreground = ${xrdb:color13:#555}
format-underline = #565529

###################

[module/aur-updates]
type = custom/script
exec = ~/.config/polybar/scripts/check-aur-updates.sh
interval = 3000
label = %output%
label-foreground = #FFFFFF
format-foreground = ${colors.foreground}
format-background = ${colors.background}
format-prefix = ": "
click-right = exec xfce4-power-manager &
click-left = exec xfce4-terminal &
format-prefix-foreground = ${xrdb:color13:#555}
format-underline = #565529

###################

[module/all-updates]
type = custom/script
exec = ~/.config/polybar/scripts/check-all-updates.sh
interval = 3000
label =  %output%
label-foreground = #FFFFFF
format-foreground = ${colors.foreground}
format-background = ${colors.background}
format-prefix = " : "
click-right = exec xfce4-power-manager
click-left = exec xfce4-terminal &
format-prefix-foreground = ${xrdb:color6:#222}
format-underline = #565529

############################################################################################################################################################

[module/backlight]
type = internal/backlight

; Use the following command to list available cards:
; $ ls -1 /sys/class/backlight/
card = intel_backlight

; Enable changing the backlight with the scroll wheel
; NOTE: This may require additional configuration on some systems. Polybar will
; write to `/sys/class/backlight/${self.card}/brightness` which requires polybar
; to have write access to that file.
; DO NOT RUN POLYBAR AS ROOT. 
; The recommended way is to add the user to the
; `video` group and give that group write-privileges for the `brightness` file.
; See the ArchWiki for more information:
; https://wiki.archlinux.org/index.php/Backlight#ACPI
; Default: false
enable-scroll = true
enable-click = true

; Available tags:
;   <label> (default)
;   <ramp>
;   <bar>
format = <ramp> <label>
#format-prefix =  " "
format-prefix-foreground = ${xrdb:color13:#555}
format-prefix-background = ${colors.background}
;format-prefix-overline = #
format-prefix-underline = #565529

; Available tokens:
;   %percentage% (default)
label = %percentage%%
format-foreground = ${colors.foreground}

format-background = ${colors.background}
;format-overline = #3b3a00
format-underline = #565529

; Only applies if <ramp> is used
ramp-0 =  
ramp-1 = 
ramp-2 = 
ramp-3 = 
ramp-4 =  
ramp-5 = 
ramp-6 = 
ramp-7 = 
ramp-foreground = #FFFFFF

; Only applies if <bar> is used
bar-width = 7
bar-indicator = 
bar-fill = .
bar-empty = .

############################################################################################################################################################

[module/battery]
;https://github.com/jaagr/polybar/wiki/Module:-battery
type = internal/battery
battery = BAT0
adapter = AC0
full-at = 100

format-charging = <animation-charging><label-charging>
label-charging =  %percentage%%
format-charging-foreground = ${xrdb:color1:#555}
format-charging-background = ${colors.background}
;format-charging-overline = #3b3a00
format-charging-underline = #565529

format-discharging = <ramp-capacity><label-discharging>
label-discharging = %percentage%%
format-discharging-foreground = ${xrdb:color1:#555}
format-discharging-background = ${colors.background}
;format-discharging-overline = #3b3a00
format-discharging-unliderline = #3b3a00

format-full-prefix = " :"
format-full-prefix-foreground = ${xrdb:color1:#555}
format-full-foreground = ${colors.foreground}
format-full-background = ${colors.background}
;format-full-overline = #3b3a00
format-full-underline = ${xrdb:color10:#555}


ramp-capacity-0 =  :
ramp-capacity-1 =  :
ramp-capacity-2 =  :
ramp-capacity-3 =  :
ramp-capacity-4 =  :
ramp-capacity-foreground = #888888

animation-charging-0 =  :
animation-charging-1 =  :
animation-charging-2 =  :
animation-charging-3 =  :
animation-charging-4 =  :

animation-charging-foreground = ${xrdb:color8:#222}
animation-charging-framerate = 750

############################################################################################################################################################

[module/bluetooth]
type = custom/script
exec = ~/.config/polybar/polybar-bluetooth/bluetooth.sh
interval = 2
click-left = exec blueberry 
click-right = exec ~/.config/polybar/polybar-bluetooth/toggle_bluetooth.sh
format-padding = 0
format-foreground = #1E90FF
format-background = #0000FF
format-underline = #FFFFFF
;format-overline = #3b3a00

############################################################################################################################################################

[module/cpu]
;https://github.com/jaagr/polybar/wiki/Module:-cpu
type = internal/cpu
; Seconds to sleep between updates
; Default: 1
interval = 1
format-foreground = ${colors.foreground}
format-background = ${colors.background}
format-prefix = " :"
click-left = exec xfce4-taskmanager
click-right = exec xfce4-taskmanager
format-prefix-foreground = ${xrdb:color3:#555}
;format-underline = #565529
;format-overline = #3b3a00
label-font = 3


; Available tags:
;   <label> (default)
;   <bar-load>
;   <ramp-load>
;   <ramp-coreload>
format = <label> 

format-padding = 0

; Available tokens:
;   %percentage% (default) - total cpu load
;   %percentage-cores% - load percentage for each core
;   %percentage-core[1-9]% - load percentage for specific core
label = Cpu%percentage:3%%

###################

[module/cpu-coreload]
type = internal/cpu

; Seconds to sleep between updates
; Default: 1
interval = 1

; Default: 80
; New in version 3.6.0
warn-percentage = 90
; Available tags:
;   <label> (default)
;   <bar-load>
;   <ramp-load>
;   <ramp-coreload>
format = <ramp-coreload>

; Format used when average CPU load (same as %percentage%) reaches warn-percentage
; If not defined, format is used instead.
; Available tags:
;   <label-warn>
;   <bar-load>
;   <ramp-load>
;   <ramp-coreload>
; New in version 3.6.0
format-warn = <ramp-coreload>

; Available tokens:
;   %percentage% (default) - total cpu load averaged over all cores
;   %percentage-sum% - Cumulative load on all cores
;   %percentage-cores% - load percentage for each core
;   %percentage-core[1-9]% - load percentage for specific core
label = CPU %percentage%%

; Available tokens:
;   %percentage% (default) - total cpu load averaged over all cores
;   %percentage-sum% - Cumulative load on all cores
;   %percentage-cores% - load percentage for each core
;   %percentage-core[1-9]% - load percentage for specific core
; New in version 3.6.0
label-warn = CPU %percentage%%

; Spacing (number of spaces, pixels, points) between individual per-core ramps
ramp-coreload-spacing = 0

ramp-coreload-0 = 𝍠 
ramp-coreload-1 = 𝍥
ramp-coreload-2 = 𝍦
ramp-coreload-3 = 𝍧
ramp-coreload-4 = 𝍨
ramp-coreload-5 = 𝍭 
ramp-coreload-foreground = ${xrdb:color2:#555}
format-underline = ${xrdb:color10:#222}
format-background = ${colors.background}
 
###########################################################################################################################################################

############################################################################################################################################################

[module/date]
type = internal/date

; Seconds to sleep between updates
; Default: 1.0
interval = 1.0

; See "https://en.cppreference.com/w/cpp/io/manip/put_time" for details on how to format the date string
; NOTE: if you want to use syntax tags here you need to use %%{...}
date =

; Optional time format
time = %X %Z %j
time-underline = ${xrdb:color2:#555}


; if `date-alt` or `time-alt` is defined, clicking
; the module will toggle between formats
date-alt =                                           
time-alt = %a  %b  %d, %Y   %H : %M : %S 

; Available tags:
;   <label> (default)
format = <label>
format-background = ${colors.background}
format-foreground = #FFFFFF

; Available tokens:
;   %date%
;   %time%
; Default: %date%
label = %date% %time%
label-font = 3
label-foreground = ${colors.foreground}
label-underline = ${xrdb:color2:#555}

############################################################################################################################################################

[module/filesystem]
;https://github.com/jaagr/polybar/wiki/Module:-filesystem
type = internal/fs

; Mountpoints to display
mount-0 = /

; Seconds to sleep between updates
; Default: 30
interval = 30
; Display fixed precision values
; Default: false
fixed-values = true

; Spacing between entries
; Default: 2
spacing = 2

; Available tags:
;   <label-mounted> (default)
;   <bar-free>
;   <bar-used>
;   <ramp-capacity>
format-mounted =<label-mounted>

format-mounted-prefix = " :"
format-mounted-prefix-foreground = ${xrdb:color10:#555}
;click-right = exec missioncenter
format-mounted-foreground = ${colors.foreground}
format-mounted-background = ${colors.background}
;format-mounted-overline = #3b3a00
format-mounted-underline = ${xrdb:color9:#555}

; Available tokens:
;   %mountpoint%
;   %type%
;   %fsname%
;   %percentage_free%
;   %percentage_used%
;   %total%
;   %free%
;   %used%
; Default: %mountpoint% %percentage_free%%
label-mounted =%percentage_used%

; Available tokens:
;   %mountpoint%
; Default: %mountpoint% is not mounted
label-unmounted = %mountpoint% not mounted
format-unmounted-foreground = ${colors.foreground}
format-unmounted-background = ${colors.background}
format-unmounted-underline = ${colors.alert}
click-right = exec xfce4-appfinder missioncenter

########                                 ;                     #####################################################################

[module/apps]
type = custom/script
interval = 1
exec = echo  ":App"
format-prefix = " "
format-prefix-foreground= ${xrdb:color13:#222}
click-left = exec gmrun &
click-right = exec xfce4-appfinder 
format-foreground = ${colors.foreground}
format-background = ${colors.background}
format-underline = ${xrdb:color13:#222}

####################

[module/que]
type = custom/script
interval = 1204
exec = echo ":Que"
format-prefix = """
format-prefix-foreground = ${xrdb:color14:#222}
click-right = exec audacious %U
click-left = exec missioncenter
format-foreground = ${colors.foreground}
format-background = ${colors.background}
format-underline = ${xrdb:color1:#222}

###################

[module/liv]
type = custom/script
interval = 1204
exec = echo ":Liv"
format-prefix = " "
format-prefix-foreground = ${xrdb:color5:#555}
;click-left = exec wal -i /home/asifbe4/Backgrounds/Wal-background900/-background.jpg
;click-right = exec wal -i /home/asifbe4/Backgrounds/Wal-background1/870-background.jpg
format-foreground = ${colors.foreground}
format-background = ${colors.background}
format-underline = ${xrdb:color8:#555}

####################

[module/rof]
type = custom/script
interval = 1
exec = echo  ":Rof"
format-prefix = " "
format-prefix-foreground = #E6E6FA 
click-left = exec ~/.config/rofi/launchers/type-4/launcher.sh
click-right = exec alacritty -e "ranger" &
format-foreground = ${colors.foreground}
format-background = ${colors.background}
format-underline = ${xrdb:color3:#222}

####################

[module/squ]
type = custom/script
interval = 1204
exec = echo " :"
format-prefix = ""
format-prefix-foreground = ${xrdb:color3:#222}
click-left = exec thunderbird %U
;click-right =
format-foreground = ${colors.foreground}
format-background = ${colors.background}
format-underline = ${xrdb:color5:#222}

####################

[module/sel]
type = custom/script
interval = 1204
exec = echo ":Sel"
format-prefix = " "
format-prefix-foreground = ${xrdb:color13:#555}
click-left = exec subl
click-right = exec geany
format-foreground = ${colors.foreground}
format-background = ${colors.background}
format-underline = ${xrdb:color1:#222}

###################

[module/wal]
type = custom/script
interval = 1
exec = echo  ":Wal"
format-prefix = " "
format-prefix-foreground= ${xrdb:color5:#222}
click-left = exec wal -i /home/asifbe4/Backgrounds/ -e
click-right = wal -R
format-foreground = ${colors.foreground}
format-background = ${colors.background}
format-underline = ${xrdb:color3:#222}

############################################################################################################################################################

[module/kernel]
type = custom/script
exec = uname -r
tail = true
interval = 1024
format-foreground = ${colors.foreground}
format-background = ${colors.background}
format-prefix = " "
enable-click = true
format-prefix-foreground = ${xrdb:color3:#222}

;format-overline = #3b3a00
format-underline = #565529

#######################################################################################🟦#🟨#🟩###################################################################

[module/launcher]
type = custom/text
content = " []= "
enable-click = true

content-background = ${colors.background}
content-foreground = ${xrdb:color7:#222}
content-underline = ${xrdb:color11:#222}
click-left = xfce4-taskmanager
click-right = exec thunar

[module/launcher1]
type = custom/text
content = " LM_Studio:"
enable-click = true

content-background = ${colors.background}
content-foreground = ${xrdb:color6:#222}
content-underline = ${xrdb:color13:#222}
click-left = exec /home/asifbe4/Applications/lmstudio-appimage-0.2.31_4b1240cbbe441a14c4baf8280f7f8594.AppImage --no-sandbox %U

[module/launcher2]
type = custom/text
content = "🟦"
enable-click = true

content-background = ${colors.background}
content-foreground = ${xrdb:color7:#222}
content-underline = ${xrdb:color9:#222}

[module/launcher3]
type = custom/text
content = "🟨"
enable-click = true

content-background = ${colors.background}
content-foreground = ${xrdb:color7:#222}
content-underline = ${xrdb:color9:#222}

[module/launcher4]
type = custom/text
content = "🟩"
enable-click = true

content-background = ${colors.background}
content-foreground = ${xrdb:color7:#222}
content-underline = ${xrdb:color9:#222}

############################################################################################################################################################

[module/memory]
;https://github.com/jaagr/polybar/wiki/Module:-memory
type = internal/memory
interval = 1
; Available tokens:
;   %percentage_used% (default)
;   %percentage_free%
;   %gb_used%
;   %gb_free%
;   %gb_total%
;   %mb_used%
;   %mb_free%
;   %mb_total%
label = %gb_used%
format = <label>
format-prefix = ""
format-prefix-foreground= #E6E6FA
format-underline = ${xrdb:color6:#222}
;format-overline = #3b3a00
format-foreground = ${colors.foreground}
format-background = ${colors.background} 

############################################################################################################################################################

[module/networkspeedup]
;https://github.com/jaagr/polybar/wiki/Module:-network
type = internal/network
;interface = wlp2s0
;interface = enp14s0
interface = wlan0

label-connected = "%upspeed:7%" 
format-connected = <label-connected>
;format-connected-prefix = "U:"
format-connected-prefix-foreground = #868686
format-connected-foreground = ${colors.foreground}
format-connected-background = ${colors.background}
format-connected-underline = #565529
#format-connected-overline = ${xrdb:color13:#555}

[module/networkspeeddown]
;https://github.com/jaagr/polybar/wiki/Module:-network
type = internal/network
;interface = wlp2s0
;interface = enp14s0
interface = wlan0
;interface = enp4s0

label-connected = "%downspeed:7%"
format-connected = <label-connected>
;format-connected-prefix = "D:"
format-connected-prefix-foreground = #868686
format-connected-foreground = ${xrdb:color10:#555}
format-connected-background = ${colors.background}
format-connected-underline = #565529
;format-connected-overline = #3b3a00

############################################################################################################################################################

[module/pavolume]
type = custom/script
tail = true
label = %output%
format-lable-foreground = ${xrdb:color6:#555}
exec = ~/.config/polybar/scripts/pavolume.sh --listen
click-right = exec pavucontrol
click-left = ~/.config/polybar/scripts/pavolume.sh --togmute
scroll-up = ~/.config/polybar/scripts/pavolume.sh --up
scroll-down = ~/.config/polybar/scripts/pavolume.sh --down
format-underline = ${xrdb:color3:#555}
format-foreground = ${colors.foreground}
format-background = ${colors.background}

############################################################################################################################################################

[module/popup-calendar]
type = custom/script
exec = ~/.config/polybar/scripts/popup-calendar.sh
interval = 5
format-prefix = ":"
format-prefix-foreground = ${xrdb:color13:#555}
format-prefix-underline = #565529
format-foreground = ${colors.foreground}
format-background = ${colors.background}
label-underline= #565529
click-left = ~/.config/polybar/scripts/popup-calendar.sh --popup &

############################################################################################################################################################

[module/pub-ip]
type = custom/script
exec = ~/.config/polybar/scripts/pub-ip.sh
interval = 100
format-foreground = ${colors.foreground}
format-background = ${colors.background}
format-underline = #565529
;format-overline = #3b3a00
label = %output%
format-prefix = ":"
format-prefix-foreground = #888888

############################################################################################################################################################

[module/release]
type = custom/script
exec = (lsb_release -d | awk {'print $2'} ;echo " "; lsb_release -r | awk {'print $2'}) | tr -d '\n'

interval = 6000
;click-left = exec urxvt -hold -e /usr/bin/inxi -F
click-left = exec xfce4-terminal -e "htop" &
format-foreground = ${colors.foreground}
format-background = ${colors.background}
format-prefix = ":"
format-prefix-foreground = ${xrdb:color13:#555}
format-overline = #3b3a00

############################################################################################################################################################

[module/sep]
type = custom/text
content = " | "
content-foreground = ${colors.sep}
content-background = ${colors.background}
content-underline  = ${colors.background}

format-foreground  = ${colors.foreground}
format-background  = ${colors.background}
format-underline   = ${colors.background}

###########################################################################################################################################

[module/temperature]
;https://github.com/jaagr/polybar/wiki/Module:-temperature
type = internal/temperature
; Thermal zone to use
; To list all the zone types, run
; $ for i in /sys/class/thermal/thermal_zone*; do echo "$i: $(<$i/type)"; done
; Default: 0
thermal-zone = 0

; Full path of temperature sysfs path
; Use `sensors` to find preferred temperature source, then run
; $ for i in /sys/class/hwmon/hwmon*/temp*_input; do echo "$(<$(dirname $i)/name): $(cat ${i%_*}_label 2>/dev/null || echo $(basename ${i%_*})) $(readlink -f $i)"; done
; to find path to desired file
; Default reverts to thermal zone setting

hwmon-path = /sys/class/thermal/thermal_zone3: INT3400 Thermal

warn-temperature = 90
format = <ramp><label>
format-foreground = ${colors.foreground}
format-background = ${colors.background}
format-underline = #565529
format-warn = <ramp><label>
format-warn-underline = #FFFF00
label = :%temperature% 
label-warn =%temperature%
label-warn-foreground = #000000

ramp-0 = 𝍥    
ramp-1 = 𝍦    
ramp-2 = 𝍧    
ramp-3 = 𝍨     
ramp-4 = 𝍤   
ramp-foreground = ${xrdb:color5:#555}

################################################################################

[module/temperature2]
type = custom/script
exec = ~/.config/polybar/scripts/tempcores.sh
interval = 2
format-padding = 1
format-foreground = ${colors.foreground}
format-background = ${colors.background}
format-underline = ${xrdb:color5:#555}
format-prefix-foreground = ${xrdb:color8:#555}
label = %output:0:150:%

############################################################################################################################################################

[module/nvme0]
;https://github.com/jaagr/polybar/wiki/Module:-temperature
type = internal/temperature
; Default: 0
thermal-zone = 0
hwmon-path = /sys/devices/platform/coretemp.0/hwmon/hwmon8/temp2_input

warn-temperature = 60
format = <ramp><label>
format-foreground = ${colors.foreground}
format-background = ${colors.background}
format-underline = #565529
format-warn = <ramp><label>
format-warn-underline = #FFFF00
label =:%temperature% 
label-warn =  %temperature%
label-warn-foreground = #000000

ramp-0 = 𝍥
ramp-1 = 𝍦    
ramp-2 = 𝍧    
ramp-3 = 𝍨     
ramp-4 = 𝍤   
ramp-foreground = ${xrdb:color13:#555}

############################################################################################################################################################

[module/uptime]
;https://github.com/jaagr/polybar/wiki/User-contributed-modules#uptime
type = custom/script
exec = uptime | awk -F, '{sub(".*up ",x,$1);print $1}'
interval = 100
label =%output%
format-foreground = ${colors.foreground}
format-background = ${colors.background}
format-prefix =":"  
format-prefix-foreground = ${xrdb:color13:#555}
format-underline = ${xrdb:color5:#222}

click-left = exec alacritty -e "bashtop" &

############################################################################################################################################################

[module/wired-network]
;https://github.com/jaagr/polybar/wiki/Module:-network
type = internal/network
;interface = enp8s0
interface = enp0s20f0u4
interval = 3.0

; Available tokens:
;   %ifname%    [wireless+wired]
;   %local_ip%  [wireless+wired]
;   %essid%     [wireless]
;   %signal%    [wireless]
;   %upspeed%   [wireless+wired]
;   %downspeed% [wireless+wired]
;   %linkspeed% [wired]
; Default: %ifname% %local_ip%
label-connected =  %ifname%
label-disconnected = %ifname% disconnected

format-connected-foreground = ${colors.foreground}
format-connected-background = ${colors.background}
;format-connected-underline = #565529
;format-connected-overline = #3b3a00
format-connected-prefix = " ﲈ"
format-connected-prefix-foreground = ${colors.foreground}
format-connected-prefix-background = ${colors.background}

format-disconnected = <label-disconnected>
format-disconnected-underline = ${colors.alert}
label-disconnected-foreground = ${colors.foreground}

############################################################################################################################################################

[module/wireless-network]
;https://github.com/jaagr/polybar/wiki/Module:-network #5FB0FF
type = internal/network
interface = wlan0
interval = 1.0

;label-connected = %{A1:xfce4-terminal -e nmtui &:} %downspeed:9%%{A} %essid% 
label-connected =%{A1:alacritty-e nmtui &:}%downspeed:9% 

format-connected =<ramp-signal><label-connected>
format-connected-foreground = ${colors.foreground}
format-connected-background = ${colors.background}
format-connected-underline = ${xrdb:color3:#222}
format-connected-prefix-foreground = ${xrdb:color6:#222}
format-connected-prefix-background = ${colors.background}

label-disconnected =Offline
label-disconnected-foreground = ${xrdb:color8:#555}
label-disconnected-background = ${colors.background}
label-disconnected-underline = #4D4D4D
click-left = nm-applet

format-disconnected-background = ${colors.background}
format-disconnected-prefix =":"
format-disconnected-prefix-foreground = ${xrdb:color14:#222}
format-disconnected-prefix-background = ${colors.background}
format-disconnected-prefix-underline = #4D4D4D

ramp-signal-0 = 𝍥: 
ramp-signal-1 = 𝍥:
ramp-signal-2 = 𝍦:  
ramp-signal-3 = 𝍧:  
ramp-signal-4 = 𝍧:
ramp-signal-5 = 𝍨:
ramp-signal-foreground = ${xrdb:color6:#555}
ramp-signal-underline = ${xrdb:color3:#222}

############################################################################################################################################################

[module/variety]
type = custom/script
exec = echo " :"
interval = 1
tail = true
format-foreground = ${xrdb:color4:#222}
format-background = ${colors.background}
format-prefix = ""
format-prefix-foreground = ${xrdb:color6:#222}
format-underline = ${xrdb:color2:#222}
click-right = variety --next
click-left = variety --previous

############################################################################################################################################################

[module/xkeyboard]
;https://github.com/jaagr/polybar/wiki/Module:-xkeyboard   "󾓦:"
type = internal/xkeyboard

; Available tags:
;   <label-layout> (default)
;   <label-indicator> (default)
format = <label-layout> <label-indicator>
format-spacing = 0

; Available tokens:
;   %layout%
;   %name%
;   %number%
;   %icon%
;   %variant% (New in version 3.6.0)
; Default: %layout%
label-layout = %icon%
label-layout-padding = 
label-layout-background = #CC2E3440
label-layout-foreground = ${xrdb:color8#222}

; Available tokens:
;   %name%
; Default: %name%
; @deprecated, use label-indicator-on
label-indicator = %name%

; layout-icon-[0-9]+ = layout;icon
; layout-icon-[0-9]+ = layout;variant;icon (New in version 3.6.0)
; Assign each layout an icon that will be available as %icon% token for the
; <label-layout> tag.
; In the first configuration form, the `layout` will try to match %layout%
; value ignoring the variant
; In the second configuration form, 
;     the `layout` will try to match %layout% value and the `variant` matches %variant%.
;     the `variant` can be empty (layout;;icon) to match against a %layout%
;                   with an empty %variant%
;     the `variant` can be the wildcard '_' to match a %layout% and any variant. 
;         This is equivalent to the first configuration form
;     the `layout` cannot be empty (;variant;icon)
;     the `layout` can be the wildcard '_' to match any %layout% with a
;                  specific %variant%
;     Do not use the wildcard '_' for both `layout` and `variant`
layout-icon-default = 'layout'
layout-icon-0 = ch;C
layout-icon-1 = us;󾓦:US

; indicator-icon-[0-9]+ = indicator;off icon;on icon
; Provides the content of the %icon% token in label-indicator-*
indicator-icon-default = 9
indicator-icon-0 = caps lock;-CL;+CL
indicator-icon-1 = scroll lock;;+SL
indicator-icon-2 = num lock;-NL;+NL

; Replaces label-indicator
; Available tokens:
;   %name%
;   %icon%
; Default: %name%
label-indicator-on = +%name%

; Available tokens:
;   %name%
;   %icon%
; Default: (none)
label-indicator-off = -%name%

; Replaces label-indicator-* if for specific indicators
; Available tokens:
;   %name%
;   %icon%
label-indicator-on-capslock = %name%
label-indicator-off-capslock = 
label-indicator-on-numlock = 
label-indicator-off-numlock = %name%
label-indicator-on-scrolllock = 
label-indicator-off-scrolllock = 

############################################################################################################################################################
 
[module/weather]
type = custom/script
interval = 20
format = <label>
format-prefix ="󰥗::"
format-prefix-foreground = ${xrdb:color8:#222}
format-underline = ${xrdb:color4:#222}
format-foreground = ${colors.foreground}
format-background = ${colors.background}
exec = curl -Ss 'https://wttr.in?0&T&Q' | cut -c 16- | head -2 | xargs echo
tail = true
click-left = exec kweather
#click-left = exec gapplication launch org.gnome.Weather
#click-left = exec /usr/bin/flatpak run --branch=stable --arch=x86_64 --command=kweather org.kde.kweather

###################

[module/xweather]
type = custom/script
interval = 20
format = <label>
format-prefix ="󰥗::"
format-prefix-foreground = ${xrdb:color6:#222}
format-underline = ${xrdb:color4:#222}
format-foreground = ${colors.foreground}
format-background = ${colors.background}
exec = curl -Ss 'https://wttr.in?0&T&Q' | cut -c 16- | head -2 | xargs echo
tail = true
click-left = exec kweather
#click-left = exec gapplication launch org.gnome.Weather
#click-left = exec /usr/bin/flatpak run --branch=stable --arch=x86_64 --command=kweather org.kde.kweather

############################################################################################################################################################

[module/xworkspaces]
type = internal/xworkspaces

pin-workspaces = false
enable-click = true
enable-scroll = true
reverse-scroll = true

icon-0 = 1;
icon-1 = 2;
icon-2 = 3;
icon-3 = 4;
icon-4 = 5;
#icon-5 = 6;
format = <label-state>
label-monitor = %name%

label-active = %index%
label-active-foreground = ${colors.active}
label-active-background = ${colors.background}
label-active-underline= #6790eb
label-active-padding = 2

label-occupied = %index%
label-occupied-foreground = ${colors.foreground}
label-occupied-background = ${colors.background}
label-occupied-padding = 2

label-urgent = %index%
label-urgent-foreground = ${colors.foreground}
label-urgent-background = ${colors.alert}
label-urgent-underline = ${colors.alert}
label-urgent-padding = 2

label-empty = %index%
label-empty-foreground = ${colors.foreground}
label-empty-padding = 2
format-foreground = ${colors.foreground}
format-background = ${colors.background}

############################################################################################################################################################

[module/xwindow]
;https://github.com/jaagr/polybar/wiki/Module:-xwindow
type = internal/xwindow
interval = 10

; Available tags:
;   <label> (default)
format = <label>
format-background = ${colors.background}
format-foreground = #FFFFFF
format-prefix = ""
format-prefix-foreground = ${xrdb:color6:#222}
format-padding = 
format-underline = ${xrdb:color3:#222}

; Available tokens:
;   %title%
; Default: %title%
label = %title% 
label-maxlen = 50

; Used instead of label when there is no window title
; Available tokens:
;   None
label-empty = 🔱:Asifbe4live Studios
label-empty-foreground = ${xrdb:color13:#222}
label-underline = ${xrdb:color1:#555}
tail = true

